// @flow

/**
 * The height of the filmstrip in narrow aspect ratio, or width in wide.
 */
export const FILMSTRIP_SIZE = 90;

/**
 * The aspect ratio of a tile in tile view.
 */
export const TILE_ASPECT_RATIO = 16 / 9;


/**
 * The margin for each side of the tile view. Taken away from the available
 * height and width for the tile container to display in.
 *
 * @private
 * @type {number}
 */
export const TILE_MARGIN = 2;

/**
 * The aspect ratio the tiles should display in.
 *
 * @private
 * @type {number}
 */
export const THUMBNAIL_ASPECT_RATIO = 1;
export const PHONE_GALLERY_COLUMN_COUNT = 2;
export const TABLET_GALLERY_COLUMN_COUNT = 6;
